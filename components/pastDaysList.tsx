import { Entry } from '@/utils/types'

//TODO: tener en cuenta la primera vez que se inicia la app, no hay entradas de ayer ni antes de ayer

const pastDaysList = ({ allEntries }: { allEntries: Entry[] }) => {
  return (
    <div>pastDaysList</div>
  )
}

export default pastDaysList